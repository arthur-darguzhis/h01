import {CommentatorInfo} from "../../../domain/types/CommentType";

export type CommentViewModel = {
    id: string
    content: string
    userId: string
    userLogin: string
    createdAt: string
}
